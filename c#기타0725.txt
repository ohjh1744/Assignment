기본실습:

-문자열을 자주 수정하거나, 많은 량의 문자를 사용한다면
string의 불변성에 의해 힙에 메모리누수가 발생할 수 있다.
-따라서 c#에선sring말고도 StringBuilder라는 클래스를 지원해준다.
-StringBuilder는 문자 수를 확장할 수 있는 동적 개체이며, 보관할 수 있는
최대 문자 수 값을 지정 가능하다.
-> 최대 문자 수 값이 개체의 용량이 되며, 이 값을 넘기지 않는 한 수정한다해도
새로운 메모리를 할당하지 않는다.
